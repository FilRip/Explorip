<Popup x:Class="Explorip.TaskBar.Controls.PopupPreviewContent"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
       xmlns:local="clr-namespace:Explorip.TaskBar.Controls"
       xmlns:config="clr-namespace:ExploripConfig.Configuration;assembly=ExploripConfig"
       xmlns:colors="clr-namespace:ExploripSharedCopy.Constants;assembly=ExploripSharedCopy"
       xmlns:viewmodels="clr-namespace:Explorip.TaskBar.ViewModels"
       xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
       mc:Ignorable="d" AllowsTransparency="True" StaysOpen="False"
       d:DesignHeight="450" d:DesignWidth="800" IsOpen="{Binding Visible}">

    <Popup.DataContext>
        <viewmodels:PopupPreviewContentViewModel/>
    </Popup.DataContext>
    
    <Border BorderThickness="0" CornerRadius="{x:Static config:ConfigManager.PopUpCornerRadius}">
        
        <ListView Margin="{x:Static config:ConfigManager.PopUpCornerRadius}" SelectionMode="Single" BorderThickness="0"
                  Background="Transparent" Foreground="{x:Static colors:Colors.ForegroundColorBrush}"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled"
                  ItemsSource="{Binding ListItems}">
            
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="MouseLeftButtonDown">
                                <i:InvokeCommandAction Command="{Binding LaunchCommand}"/>
                            </i:EventTrigger>
                            <i:EventTrigger EventName="MouseRightButtonDown">
                                <i:InvokeCommandAction Command="{Binding ContextMenuCommand}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <Image Source="{Binding SmallIcon}" Margin="5"/>
                        <TextBlock Text="{Binding DisplayName}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Border>
    
</Popup>
