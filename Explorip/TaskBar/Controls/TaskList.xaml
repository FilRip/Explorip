<UserControl x:Class="Explorip.TaskBar.Controls.TaskList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:local="clr-namespace:Explorip.TaskBar.Controls"
             xmlns:viewmodels="clr-namespace:Explorip.TaskBar.ViewModels"
             Loaded="TaskList_OnLoaded" Unloaded="UserControl_Unloaded"
             Background="Transparent" Visibility="Visible">
    
    <UserControl.DataContext>
        <viewmodels:TaskListViewModel/>
    </UserControl.DataContext>
    
    <ScrollViewer Style="{DynamicResource TaskListScrollViewer}"
                  Focusable="False"
                  VerticalScrollBarVisibility="Auto">
        
        <ListView x:Name="TasksList" SelectionMode="Single"
                 ItemsSource="{Binding TaskListCollection}"
                 BorderThickness="0" Background="Transparent"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 HorizontalAlignment="Stretch">
            
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="{Binding DataContext.PanelOrientation, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:TaskList}}"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="{Binding Path=DataContext.ButtonWidth, RelativeSource={RelativeSource FindAncestor, AncestorType=local:TaskList}, UpdateSourceTrigger=PropertyChanged}" MaxWidth="{Binding Path=DataContext.TaskButtonLength, RelativeSource={RelativeSource FindAncestor, AncestorType=local:TaskList}, UpdateSourceTrigger=PropertyChanged}"/>
                            <ColumnDefinition Width="{Binding Path=DataContext.ButtonRightMargin, RelativeSource={RelativeSource FindAncestor, AncestorType=local:TaskList}, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="{Binding Path=DataContext.ButtonHeight, RelativeSource={RelativeSource FindAncestor, AncestorType=local:TaskList}, UpdateSourceTrigger=PropertyChanged}"/>
                            <RowDefinition Height="{Binding Path=DataContext.ButtonBottomMargin, RelativeSource={RelativeSource FindAncestor, AncestorType=local:TaskList}, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid.RowDefinitions>
                        <local:TaskButton/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </ScrollViewer>
</UserControl>
