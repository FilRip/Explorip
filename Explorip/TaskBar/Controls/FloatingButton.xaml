<UserControl x:Class="Explorip.TaskBar.Controls.FloatingButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Explorip.TaskBar.Controls"
             xmlns:constants="clr-namespace:Explorip.Constants"
             xmlns:viewmodels="clr-namespace:Explorip.TaskBar.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="UserControl_Loaded">
    <UserControl.DataContext>
        <viewmodels:FloatingButtonViewModel/>
    </UserControl.DataContext>
    <ToggleButton Width="16" HorizontalAlignment="Stretch" VerticalAlignment="Top" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Focusable="False"
                  Style="{DynamicResource FloatingButton}"
                  ToolTip="{DynamicResource FloatingButtonTip}"
                  Command="{Binding ExpandTaskbarCommand}">
        <Image x:Name="FloatingButtonImage" Source="{x:Static constants:Icons.ExpandButton}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Width="16"/>
    </ToggleButton>
</UserControl>
