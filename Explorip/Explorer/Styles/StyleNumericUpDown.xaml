<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:local="clr-namespace:Explorip.Explorer.Helpers"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:colors="clr-namespace:ExploripSharedCopy.Constants;assembly=ExploripSharedCopy">

    <Style TargetType="{x:Type local:NumericUpDown}" x:Key="NumericUpDownStyle">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:NumericUpDown}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <RepeatButton Grid.Row="0" Name="Part_UpButton" Content="▲"/>
                        <TextBox Grid.Row="1"
                                 HorizontalContentAlignment="Center"
                                 BorderThickness="0"
                                 Background="{x:Static colors:Colors.BackgroundColorBrush}"
                                 Foreground="{x:Static colors:Colors.ForegroundColorBrush}"
                                 Name="Part_TextBox"/>
                        <RepeatButton Grid.Row="2" Name="Part_DownButton" Content="▼"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
