namespace ExploripComponents;

public static class Constants
{
    public const int DelayIgnoreDrag = 200;
}

public enum GroupBy
{
    NONE = 0,
    NAME = 1,
    SIZE = 2,
    TYPE = 3,
    LAST_MODIFIED = 4,
}
