<UserControl x:Class="ConsoleControl.WPF.ConsoleControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:consoleControl="clr-namespace:ConsoleControl.WPF"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">

    <!-- The internal rich text box for the console. -->
    <RichTextBox
        x:Name="richTextBoxConsole" Foreground="White" AcceptsTab="True" PreviewKeyUp="RichTextBoxConsole_PreviewKeyUp" FontFamily="Consolas" FontSize="12"
        Background="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type consoleControl:ConsoleControl}}, Path=Background}"
        CaretBrush="White" PreviewKeyDown="RichTextBoxConsole_PreviewKeyDown" KeyUp="RichTextBoxConsole_KeyUp" VerticalScrollBarVisibility="Visible">
        <RichTextBox.Resources>
            <Style TargetType="{x:Type Paragraph}">
                <Setter Property="Margin" Value="0"/>
            </Style>
        </RichTextBox.Resources>
    </RichTextBox>
</UserControl>
